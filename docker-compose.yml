version: "3.9"

services:

  x402-notify:

    build: .

    container_name: x402-notify-service

    restart: unless-stopped

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app
